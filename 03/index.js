const ladoA = 2;
const ladoB = 2;

//seu código aqui

// Resolução utilizando Ternário.
//const bucha = ladoA === ladoB ? "SIM" : "NÃO";
//console.log(bucha);

// Resolução utilizando o IF e Else.

if (ladoA === ladoB) {
    console.log("SIM")
} else {
    console.log("NÃO")
}