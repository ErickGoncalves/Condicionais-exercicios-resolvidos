const ladoA = 3;
const ladoB = 2;

//seu código aqui

// Resolução utilizando o E(&&) Lógico.
if (ladoA === ladoB && ladoA === 3) {
    // Só imprimirá algo na tela se as duas condições acima forem true;
    console.log("Terno");
}

// vResolução utilizando encadeamento do IF
/*
if (ladoA === ladoB) {
    if (ladoA === 3) {
        console.log("Terno");
    }
} */